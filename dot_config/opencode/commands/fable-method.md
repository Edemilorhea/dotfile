---
description: Run a task through the explicit command-only Fable Method
agent: FableAgent
subtask: false
---

# Fable Method

FABLE_COMMAND_ENTRY: /fable-method

Task: `$ARGUMENTS`.

This is an explicit Fable command entry. Use `Read`, not the `skill` tool, to load the exact home-relative payload `~/.config/opencode/config/assets/fable/fable-method/METHOD.md`. Follow it as the sole workflow authority. Follow nested references with `Read` only while this command is active.
