---
description: Judge completed work through the explicit command-only Fable Judge
agent: FableAgent
subtask: false
---

# Fable Judge

FABLE_COMMAND_ENTRY: /fable-judge

Target: `$ARGUMENTS`.

This is an explicit Fable command entry. Use `Read`, not the `skill` tool, to load the exact home-relative payload `~/.config/opencode/config/assets/fable/fable-judge/JUDGE.md`. If that payload requires method fraud catalogues or domain adapters, follow its exact paths with `Read` only while this command is active. Judge only; do not modify the work unless the user separately asks.
