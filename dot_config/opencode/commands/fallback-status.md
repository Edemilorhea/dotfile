---
description: Show opencode-auto-fallback status (V1 only; the plugin is not ported to V2)
---

Call the `fallback-status` tool and display its full output. If the tool is unavailable, say so in one line; do not simulate its output.
