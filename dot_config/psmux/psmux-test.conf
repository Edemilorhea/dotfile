# 測試配置 - 簡化的顏色設定

# 終端設定
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

# 滑鼠設定 - 關閉 tmux 滑鼠模式，讓終端機原生處理選取
set -g mouse off

# 狀態列
set -g status on
set -g status-position bottom
set -g status-style "bg=colour233,fg=colour250"

# 左側 (使用簡單的顏色)
set -g status-left "#[bg=colour24,fg=colour0,bold] [#S] #[bg=colour233,fg=colour250]#I:#W "

# 右側
set -g status-right "#[bg=colour233,fg=colour250,bold] %Y-%m-%d %H:%M:%S "

# 視窗狀態
set -g window-status-format "#[fg=colour240,bg=colour233] #I:#W#F "
set -g window-status-current-format "#[fg=colour24,bg=colour233,bold] #I:#W#F "
set -g window-status-separator "#[fg=colour240,bg=colour233]│"
